i watch dub here
https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1

it have 3 servers for dub
Vidplay
MyCloud
Cloudora

if any more things you needed of that site i will send like Inspect information or something which will help you more

19 minutes ago

Worked for 9 seconds
Good info. Let me look at how aniwaves.ru serves its streams so I can build around it.


Show less

Executed curl -s "https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1" -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" -L | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const html=Buffer.concat(c).toString(); console.log('Length:', html.length); const titleM=html.match(/<title>(.*?)<\/title>/); console.log('Title:', titleM?.[1]); // Look for anime ID, episode sources const idM=html.match(/animeId[\"'\s:=]+([\\d]+)/i); console.log('animeId:', idM?.[1]); const epM=html.match(/episodeId[\"'\s:=]+([\\d]+)/i); console.log('episodeId:', epM?.[1]); // Look for API calls const apiMatches=html.match(/[\"'](\/api\/[^\"']+)[\"']/g); if(apiMatches) console.log('API refs:', [...new Set(apiMatches)].slice(0,10)); // Look for script tags const scripts=html.match(/<script[^>]*src=[\"']([^\"']+)[\"']/g); if(scripts) console.log('Scripts:', scripts.slice(0,8)); });" 2>/dev/null

Executed curl -s "https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1" -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" -L | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const html=Buffer.concat(c).toString(); // Look for episode/server data in JSON blobs const jsonBlobs=html.match(/\{[^{}]{100,5000}\}/g)||[]; jsonBlobs.slice(0,5).forEach((b,i)=>console.log('JSON blob',i,':', b.slice(0,200))); // Look for server/source iframe links const iframes=html.match(/iframe[^>]*src=[\"']([^\"']+)[\"']/gi); if(iframes) console.log('Iframes:', iframes.slice(0,5)); // Look for embed URLs const embeds=html.match(/https?:\/\/[^\s\"'<>]+(?:embed|player|watch)[^\s\"'<>]*/gi); if(embeds) console.log('Embed URLs:', [...new Set(embeds)].slice(0,10)); // Server list data const serverMatch=html.match(/server[^<]{0,500}/gi); if(serverMatch) console.log('Server refs:', serverMatch.slice(0,5)); });" 2>/dev/null

Executed curl -s "https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1" -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" -L | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const html=Buffer.concat(c).toString(); // Find the servers div section const idx=html.indexOf('servers\">'); if(idx>0) console.log('Servers section:', html.slice(idx, idx+2000)); // Find episode data attributes const dataEpMatches=html.match(/data-ep[^>]{0,200}/gi); if(dataEpMatches) console.log('Data-ep attrs:', dataEpMatches.slice(0,10)); // find data-id const dataId=html.match(/data-id=[\"'][^\"']+[\"']/gi); if(dataId) console.log('Data-ids:', dataId.slice(0,10)); // find any fetch/ajax calls const fetchCalls=html.match(/fetch\([\"']([^\"']+)[\"']/g); if(fetchCalls) console.log('Fetch calls:', fetchCalls.slice(0,10)); const ajaxUrl=html.match(/url\s*:\s*[\"']([^\"']+)[\"']/g); if(ajaxUrl) console.log('Ajax urls:', ajaxUrl.slice(0,10)); });" 2>/dev/null

Executed curl -s "https://aniwaves.ru/assets/js/main.js?6618798066125360" -A "Mozilla/5.0" | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const js=Buffer.concat(c).toString(); console.log('JS length:', js.length); // Find API endpoint patterns const apiPaths=js.match(/[\"'](\/ajax\/[^\"']+)[\"']/g); if(apiPaths) console.log('Ajax paths:', [...new Set(apiPaths)].slice(0,20)); // Find any URL construction const urlConst=js.match(/[\"'](\/[a-z][^\"']*(?:source|server|episode|stream)[^\"']*)[\"']/gi); if(urlConst) console.log('URL patterns:', [...new Set(urlConst)].slice(0,20)); });" 2>/dev/null

Executed curl -s "https://aniwaves.ru/ajax/v2/episode/servers?episodeId=1&animeId=80015&type=dub" -A "Mozilla/5.0" -H "Referer: https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1" -H "X-Requested-With: XMLHttpRequest" | node -e "const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>console.log(Buffer.concat(c).toString().slice(0,2000)));"

Executed curl -s "https://aniwaves.ru/ajax/anime/views/80015" -A "Mozilla/5.0" -H "Referer: https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1" | node -e "const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>console.log(Buffer.concat(c).toString().slice(0,1000)));"

Executed # Try to find the episode list endpoint curl -s "https://aniwaves.ru/ajax/v2/episode/list/80015" -A "Mozilla/5.0" -H "Referer: https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1" -H "X-Requested-With: XMLHttpRequest" | node -e "const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>console.log(Buffer.concat(c).toString().slice(0,3000)));"

Executed # Find episode IDs from the page HTML curl -s "https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1" -A "Mozilla/5.0" -L | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const html=Buffer.concat(c).toString(); // Look for episode list data const epItems=html.match(/data-ep-id=[\"'][^\"']+[\"']/gi)||[]; console.log('Episode IDs:', epItems.slice(0,10)); // Look for ep-1 specific data const watchLinks=html.match(/\/watch\/[^\s\"'<>]+/g)||[]; console.log('Watch links:', [...new Set(watchLinks)].slice(0,10)); // Any data attributes with numbers that could be ep IDs const dataNum=html.match(/data-[a-z-]+=[\s\"']?[\d]{4,}[\s\"']?/gi)||[]; console.log('Data numeric attrs:', dataNum.slice(0,10)); });" 2>/dev/null

Executed # The page loads episodes dynamically. Try the actual episode watch page curl -s "https://aniwaves.ru/watch/darling-in-the-franxx-80015/episode/1?source=dub" -A "Mozilla/5.0" -L | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const html=Buffer.concat(c).toString(); console.log('Length:', html.length); // Look for episode data const epData=html.match(/episode[_-]?(?:id|data|info)[^<\"']{0,200}/gi)||[]; console.log('Ep data:', epData.slice(0,5)); // Look for sources/servers const srcData=html.match(/source[_-]?(?:id|url|data)[^<\"']{0,200}/gi)||[]; console.log('Src data:', srcData.slice(0,5)); // Check for any inline JSON with episode data const jsonLarge=html.match(/\{(?:[^{}]|\{[^{}]*\}){50,}\}/g)||[]; jsonLarge.slice(0,3).forEach((j,i)=>{ try { const p=JSON.parse(j); if(Object.keys(p).length>2) console.log('JSON',i,':',JSON.stringify(p).slice(0,300)); } catch{} }); });" 2>/dev/null

Executed # Try common aniwave-style API patterns (this is a clone of aniwave.to) for endpoint in \ "ajax/episode/servers?id=1" \ "ajax/server/list?ep=1&t=dub" \ "ajax/anime/episode?ep=1&id=80015" \ "ajax/v2/episode/source?id=1" \ "api/episode/1/servers"; do result=$(curl -s "https://aniwaves.ru/$endpoint" -A "Mozilla/5.0" -H "Referer: https://aniwaves.ru/" -H "X-Requested-With: XMLHttpRequest" 2>/dev/null | head -c 300) echo "--- $endpoint ---" echo "$result" done

Executed # Find episode_id and episode-id usage in the page curl -s "https://aniwaves.ru/watch/darling-in-the-franxx-80015/episode/1?source=dub" -A "Mozilla/5.0" -L | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const html=Buffer.concat(c).toString(); // Find episode_id value const epIdMatch=html.match(/episode[_-]id[^<\"']{0,50}/gi); console.log('episode_id matches:', epIdMatch?.slice(0,5)); // Search for any numeric IDs near 'episode' const ctxMatch=html.match(/.{0,100}episode[_-]id.{0,100}/gi); ctxMatch?.slice(0,5).forEach(m=>console.log('CTX:', m)); // Look for Vidplay / MyCloud / Cloudora server links const serverDiv=html.indexOf('servers'); if(serverDiv>0) { const section=html.slice(serverDiv, serverDiv+3000); console.log('SERVER SECTION:\n', section); } });" 2>/dev/null

Executed # Servers are loaded dynamically. Look at JS for the actual ajax calls curl -s "https://aniwaves.ru/assets/js/main.js?6618798066125360" -A "Mozilla/5.0" | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const js=Buffer.concat(c).toString(); // Find server loading code const serverIdx=js.indexOf('server'); console.log('First server ctx:', js.slice(serverIdx-100, serverIdx+500)); // Find ajax URLs const ajaxMatches=js.match(/\$.ajax[^;]{0,500}/g)||[]; ajaxMatches.slice(0,5).forEach(m=>console.log('AJAX:', m)); // Find fetch calls const fetchMatches=js.match(/fetch\([^;]{0,300}/g)||[]; fetchMatches.slice(0,5).forEach(m=>console.log('FETCH:', m)); // Find url patterns const urlPats=js.match(/url\s*:\s*[\"'][^\"']+[\"']/g)||[]; console.log('URLs:', urlPats.slice(0,20)); });" 2>/dev/null

Executed # Look for the key functions - Ba() and Va() seem to get episode/server data curl -s "https://aniwaves.ru/assets/js/main.js?6618798066125360" -A "Mozilla/5.0" | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const js=Buffer.concat(c).toString(); // Search for 'ep-id' and 'sv-id' data attrs const epIdIdx=js.indexOf('ep-id'); console.log('ep-id context:', js.slice(epIdIdx-200, epIdIdx+500)); });" 2>/dev/null

Executed # Search for the episode server loading logic - look for w-servers or similar curl -s "https://aniwaves.ru/assets/js/main.js?6618798066125360" -A "Mozilla/5.0" | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const js=Buffer.concat(c).toString(); // Search for 'w-servers' or 'w-episodes' const idx=js.indexOf('w-server'); console.log('w-server ctx:', js.slice(idx-50, idx+1000)); });" 2>/dev/null

Executed # The JS is minified/obfuscated. Let's use Puppeteer to intercept network requests on aniwaves instead curl -s "https://aniwaves.ru/assets/js/main.js?6618798066125360" -A "Mozilla/5.0" | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const js=Buffer.concat(c).toString(); // Look for ajax calls that load servers // Search for patterns like /ajax/ or /api/ const all=js.match(/[\"'\](\/[a-z][^\"'\\s]{5,80})[\"'\]/g)||[]; const paths=[...new Set(all.map(s=>s.slice(1,-1)))].filter(p=>p.includes('ajax')||p.includes('/api/')||p.includes('server')||p.includes('episode')||p.includes('source')); console.log('API paths:', paths.slice(0,30)); });" 2>/dev/null`

Executed # Use Puppeteer to intercept all network requests on the aniwaves dub page node -e " const puppeteer = require('./node_modules/puppeteer-core'); (async () => { const browser = await puppeteer.launch({ executablePath: '/nix/store/5afrhwm7zqn1vb7p5z1mc2rkh2grsfgz-ungoogled-chromium-138.0.7204.100/bin/chromium', headless: true, args: ['--no-sandbox','--disable-setuid-sandbox','--disable-dev-shm-usage','--disable-gpu','--single-process'] }); const page = await browser.newPage(); const requests = []; page.on('request', req => { const url = req.url(); if (url.includes('aniwaves.ru') && !url.match(/\.(js|css|png|jpg|ico|woff)/)) { requests.push({ url, method: req.method() }); } }); page.on('response', async resp => { const url = resp.url(); if (url.includes('aniwaves.ru/ajax') || url.includes('aniwaves.ru/api')) { try { const text = await resp.text().catch(()=>''); console.log('RESP:', url, '->', text.slice(0,500)); } catch {} } }); await page.goto('https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1', { waitUntil: 'networkidle2', timeout: 30000 }).catch(()=>{}); await new Promise(r => setTimeout(r, 5000)); console.log('ALL REQUESTS:'); requests.forEach(r => console.log(r.method, r.url)); await browser.close(); })(); " 2>/dev/null
The key endpoints are clear now. I can see exactly how aniwaves works:

GET /ajax/episode/list/{animeId}?vrf= — lists episodes
GET /ajax/server/list?servers={animeId}&eps={episodeNum} — gets servers (sub + dub) with encoded data-link-id
Let me dig into decoding those link IDs and then build it all out.


Executed # Get the full server list response to see dub servers and their link-ids curl -s "https://aniwaves.ru/ajax/server/list?servers=80015&eps=1" -A "Mozilla/5.0" -H "Referer: https://aniwaves.ru/watch/darling-in-the-franxx-80015/ep-1" -H "X-Requested-With: XMLHttpRequest" | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const j=JSON.parse(Buffer.concat(c).toString()); const html=j.result; const{load}=require('cheerio');const \$=load(html); // Find dub servers \$('.type').each((i,el)=>{ const type=\$(el).attr('data-type'); console.log('TYPE:', type); \$(el).find('li').each((j,li)=>{ const epId=\$(li).attr('data-ep-id'); const svId=\$(li).attr('data-sv-id'); const linkId=\$(li).attr('data-link-id'); const name=\$(li).text().trim(); console.log(' Server:', name, 'ep-id:', epId, 'sv-id:', svId, 'link-id:', linkId?.slice(0,50)); }); }); });" 2>/dev/null
Now I have everything. The API gives me the dub servers with their encoded link-id. Now I need to figure out how to decode those link IDs to get the actual iframe URL to extract the stream from.


2 actions
I found it. The key is: ajax/sources?id={link-id}&asi={skip}&autoPlay={auto} — it decodes the link-id and returns the iframe URL. Let me build the full aniwaves extractor now.


7 actions
Now I have the full picture. The ajax/sources?id={link-id} returns result.url which is the iframe URL. The issue is the server returns status:false without a valid session cookie. The site needs Cloudflare clearance.

The real approach: use Puppeteer to load the page, extract the link-ids from the rendered server list, then use page.evaluate to call ajax/sources from within the same browser session (which has the right cookies/CF clearance). Let me build this into the server extractor.


Opened server/extractor.ts
Now I have a complete picture. The best approach: use Puppeteer to load the aniwaves dub page, wait for the servers to render (they load via ajax/server/list), then use page.evaluate to call ajax/sources from within the browser's session (which has the Cloudflare cookie), and extract the returned iframe URL, then intercept the stream from that iframe. Let me write this as a proper extractDubFromAniwaves function.


Edited server/extractor.ts
Now update the server route to use the aniwaves extractor instead of the broken AllAnime dub extraction, and add a search endpoint to find the aniwaves slug.


2 actions
Now replace the broken /api/extract/dub route with one that uses the new aniwaves extractor:


2 actions
Now the frontend also needs to cache the aniwaves slug to avoid searching every episode change. Let me also update the Watch component to store and reuse the aniwaves ID/slug.


Opened client/src/components/Watch.tsx
Now let me also add aniwaves state caching to the Watch component so it looks up the slug only once per anime (not on every episode change):


2 actions
Now update the StreamPanel to pass aniwaves-specific params when dub mode is active, and pass the aniwavesInfo down:


2 actions
Now pass the aniwaves info to StreamPanel in the Watch component:


3 actions
Server is up. Now let me do a quick end-to-end test of the new dub endpoint to verify it works before telling the user:


Show less

Executed curl -s "http://localhost:5000/api/extract/dub?title=my+hero+academia&episode=1" -m 90 | node -e "const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>console.log(Buffer.concat(c).toString().slice(0,400)));" & sleep 2 # Meanwhile test search curl -s "http://localhost:5000/api/gogoanime/search?q=attack+on+titan" | node -e "const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>console.log(Buffer.concat(c).toString().slice(0,300)));"

Executed # The search function returns no results - let's debug the aniwaves search curl -s "https://aniwaves.ru/search?keyword=my+hero+academia&type=1" -A "Mozilla/5.0" | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const html=Buffer.concat(c).toString(); const links=html.match(/href=\"\/watch\/[^\"]+\"/g)||[]; console.log('Watch links:', [...new Set(links)].slice(0,5)); const titles=html.match(/data-tip=\"[^\"]+\"|title=\"[^\"]+\"/g)||[]; console.log('Titles:', titles.slice(0,5)); console.log('Page title:', html.match(/<title>(.*?)<\/title>/)?.[1]); });" 2>/dev/null

Executed # Search uses different URL structure - let me check what works curl -s "https://aniwaves.ru/search?keyword=boku+no+hero+academia" -A "Mozilla/5.0" | node -e " const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{ const html=Buffer.concat(c).toString(); const links=html.match(/href=\"\/watch\/[^\"]+\"/g)||[]; console.log('Watch links:', [...new Set(links)].slice(0,5)); console.log('HTML snippet:', html.slice(5000,6500)); });" 2>/dev/null

Executed curl -sv "https://aniwaves.ru/search?keyword=naruto" -A "Mozilla/5.0" 2>&1 | grep "< HTTP\|Location\|location" | head -10

Executed # The search page returns 404 at /search. Let's try the AJAX search endpoint curl -s "https://aniwaves.ru/ajax/search?keyword=darling+in+the+franxx" -A "Mozilla/5.0" -H "X-Requested-With: XMLHttpRequest" | node -e "const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>console.log(Buffer.concat(c).toString().slice(0,500)));" 2>/dev/null echo "---" curl -s "https://aniwaves.ru/ajax/v2/search?keyword=darling" -A "Mozilla/5.0" -H "X-Requested-With: XMLHttpRequest" | node -e "const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>console.log(Buffer.concat(c).toString().slice(0,500)));" 2>/dev/null

Executed # The search is loaded dynamically via Puppeteer session. The aniwaves search needs to go through Puppeteer. # But a simpler approach: the user provides a title - let's use Puppeteer to do the search # Actually, looking at the network requests from before, the Puppeteer page loaded fine. # The key issue is: given a title, we need to get the animeId and slug. # Let's use Puppeteer to do the search on aniwaves node -e " const puppeteer = require('./node_modules/puppeteer-core'); (async () => { const browser = await puppeteer.launch({ executablePath: '/nix/store/5afrhwm7zqn1vb7p5z1mc2rkh2grsfgz-ungoogled-chromium-138.0.7204.100/bin/chromium', headless: true, args: ['--no-sandbox','--disable-setuid-sandbox','--disable-dev-shm-usage','--disable-gpu','--single-process'] }); const page = await browser.newPage(); await page.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'); // Try the search with Puppeteer intercepting AJAX results page.on('response', async resp => { const url = resp.url(); if (url.includes('search') || url.includes('suggest')) { try { const t = await resp.text(); console.log('Search resp:', url.slice(0,80), '->', t.slice(0,300)); } catch {} } }); await page.goto('https://aniwaves.ru/', { waitUntil: 'networkidle2', timeout: 20000 }).catch(()=>{}); // Try typing in the search box const input = await page.$('input[type=search], input[name=keyword], .search-input, input[placeholder*=earch]'); if (input) { await input.type('my hero academia'); await new Promise(r => setTimeout(r, 3000)); } else { console.log('No search input found'); const inputs = await page.evaluate(() => Array.from(document.querySelectorAll('input')).map(i => i.type + '|' + i.name + '|' + i.placeholder)); console.log('All inputs:', inputs); } await browser.close(); })().catch(e => console.error('Error:', e.message)); " 2>/dev/null

Executed node << 'EOF' const puppeteer = require('./node_modules/puppeteer-core'); (async () => { const browser = await puppeteer.launch({ executablePath: '/nix/store/5afrhwm7zqn1vb7p5z1mc2rkh2grsfgz-ungoogled-chromium-138.0.7204.100/bin/chromium', headless: true, args: ['--no-sandbox','--disable-setuid-sandbox','--disable-dev-shm-usage','--disable-gpu','--single-process'] }); const page = await browser.newPage(); await page.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'); page.on('response', async resp => { const url = resp.url(); if (url.includes('search') || url.includes('suggest') || url.includes('filter')) { try { const t = await resp.text(); console.log('RESP:', url.slice(0,100), '->', t.slice(0,500)); } catch {} } }); await page.goto('https://aniwaves.ru/', { waitUntil: 'networkidle2', timeout: 20000 }).catch(()=>{}); const inputs = await page.evaluate(() => Array.from(document.querySelectorAll('input')).map(i => i.type + '|' + i.name + '|' + i.placeholder + '|' + i.className)); console.log('All inputs:', inputs); // Try typing in first search-like input const searchInput = await page.$('[type="search"], [name="keyword"], [placeholder*="earch" i], .header-search input'); if (searchInput) { await searchInput.type('my hero academia'); await new Promise(r => setTimeout(r, 3000)); console.log('Typed in search input'); } else { console.log('No search input found'); } await browser.close(); })().catch(e => console.error('Error:', e.message)); EOF

Executed node << 'EOF' // The aniwaves search works via the ajax/server/list API which we already know works. // The key issue is finding the anime slug from a title. // Since we know the URL structure is /watch/{slug}-{id}/ep-{episode} // Let's try the site's filter/search API from a Puppeteer session const puppeteer = require('./node_modules/puppeteer-core'); (async () => { const browser = await puppeteer.launch({ executablePath: '/nix/store/5afrhwm7zqn1vb7p5z1mc2rkh2grsfgz-ungoogled-chromium-138.0.7204.100/bin/chromium', headless: true, args: ['--no-sandbox','--disable-setuid-sandbox','--disable-dev-shm-usage','--disable-gpu','--single-process'] }); const page = await browser.newPage(); await page.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'); await page.goto('https://aniwaves.ru/filter', { waitUntil: 'networkidle2', timeout: 20000 }).catch(()=>{}); // Try ajax search const result = await page.evaluate(async (title) => { const r = await fetch('/ajax/filter?keyword=' + encodeURIComponent(title) + '&type=1&sort=most_relevance&page=1', { headers: { 'X-Requested-With': 'XMLHttpRequest' } }); return r.json(); }, 'my hero academia'); console.log('Filter result:', JSON.stringify(result).slice(0, 1000)); await browser.close(); })().catch(e => console.error('Error:', e.message)); EOF

Executed node << 'EOF' // Try the simpler approach: search aniwaves from within Puppeteer using their search API const puppeteer = require('./node_modules/puppeteer-core'); (async () => { const browser = await puppeteer.launch({ executablePath: '/nix/store/5afrhwm7zqn1vb7p5z1mc2rkh2grsfgz-ungoogled-chromium-138.0.7204.100/bin/chromium', headless: true, args: ['--no-sandbox','--disable-setuid-sandbox','--disable-dev-shm-usage','--disable-gpu','--single-process'] }); const page = await browser.newPage(); await page.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'); await page.goto('https://aniwaves.ru/', { waitUntil: 'domcontentloaded', timeout: 20000 }).catch(()=>{}); await new Promise(r => setTimeout(r, 2000)); // Try search via fetch from within page context const r1 = await page.evaluate(async () => { const r = await fetch('/ajax/v2/filter?keyword=my+hero&type=&sort=most_relevance&page=1', { headers: { 'X-Requested-With': 'XMLHttpRequest' } }); return { status: r.status, text: await r.text().catch(()=>'') }; }); console.log('v2/filter:', r1.status, r1.text.slice(0, 300)); const r2 = await page.evaluate(async () => { const r = await fetch('/ajax/movie/search?keyword=my+hero+academia', { headers: { 'X-Requested-With': 'XMLHttpRequest' } }); return { status: r.status, text: await r.text().catch(()=>'') }; }); console.log('movie/search:', r2.status, r2.text.slice(0, 300)); await browser.close(); })().catch(e => console.error('Error:', e.message)); EOF

Executed node << 'EOF' // The filter and movie/search return 200 but empty. Let's try the actual search page URL seen in network traffic earlier. // From earlier Puppeteer: page loaded fine, ajax/server/list worked when animeId is known. // The real problem is finding the animeId from a title. // Let's try the filter page URL directly and parse it via Puppeteer const puppeteer = require('./node_modules/puppeteer-core'); (async () => { const browser = await puppeteer.launch({ executablePath: '/nix/store/5afrhwm7zqn1vb7p5z1mc2rkh2grsfgz-ungoogled-chromium-138.0.7204.100/bin/chromium', headless: true, args: ['--no-sandbox','--disable-setuid-sandbox','--disable-dev-shm-usage','--disable-gpu','--single-process'] }); const page = await browser.newPage(); await page.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'); let searchResult = null; page.on('response', async resp => { const url = resp.url(); if (url.includes('/ajax/filter')) { try { const text = await resp.text(); console.log('FILTER RESP:', url.slice(0,100), '->', text.slice(0,1000)); } catch {} } }); // Navigate to filter page with keyword await page.goto('https://aniwaves.ru/filter?keyword=my+hero+academia&type=1', { waitUntil: 'networkidle2', timeout: 25000 }).catch(()=>{}); await new Promise(r => setTimeout(r, 3000)); // Get all watch links from the page const links = await page.evaluate(() => { return Array.from(document.querySelectorAll('a[href*="/watch/"]')).map(a => ({ href: a.href, text: a.textContent.trim().slice(0, 50), title: a.getAttribute('title') })).filter(l => l.href.includes('/watch/') && !l.href.includes('#')); }); console.log('Watch links:', JSON.stringify(links.slice(0, 5))); await browser.close(); })().catch(e => console.error('Error:', e.message)); EOF

Executed node << 'EOF' // The filter page results are also dynamically loaded via JS. // The real solution: use the ajax/filter endpoint from within a valid session. // Let's try the filter endpoint that's called when the filter page renders. const puppeteer = require('./node_modules/puppeteer-core'); (async () => { const browser = await puppeteer.launch({ executablePath: '/nix/store/5afrhwm7zqn1vb7p5z1mc2rkh2grsfgz-ungoogled-chromium-138.0.7204.100/bin/chromium', headless: true, args: ['--no-sandbox','--disable-setuid-sandbox','--disable-dev-shm-usage','--disable-gpu','--single-process'] }); const page = await browser.newPage(); await page.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'); let filterHtml = null; page.on('response', async resp => { const url = resp.url(); if (url.includes('ajax/filter') || url.includes('ajax/search')) { try { const text = await resp.text(); console.log('AJAX RESP:', url.slice(0,100), 'status:', resp.status(), '->', text.slice(0,500)); filterHtml = text; } catch {} } }); await page.goto('https://aniwaves.ru/filter?keyword=my+hero+academia', { waitUntil: 'networkidle2', timeout: 25000 }).catch(()=>{}); await new Promise(r => setTimeout(r, 4000)); // Try calling ajax/filter from within the session const result = await page.evaluate(async (kw) => { const resp = await fetch('/ajax/filter?keyword=' + encodeURIComponent(kw) + '&type=&sort=most_relevance&page=1', { headers: { 'X-Requested-With': 'XMLHttpRequest' } }); return { status: resp.status, text: await resp.text() }; }, 'my hero academia'); console.log('In-page ajax/filter:', result.status, result.text.slice(0, 800)); await browser.close(); })().catch(e => console.error('Error:', e.message)); EOF
You've reached your daily free quota limit. It will reset at 12:00 AM UTC.


Checkpoint made
4 minutes ago

Worked for 11 minutes